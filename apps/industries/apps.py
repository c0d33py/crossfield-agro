from django.apps import AppConfig


class IndustriesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.industries"
    label = "industries"
    verbose_name = "Industries we serve (catalog landing pages)"
